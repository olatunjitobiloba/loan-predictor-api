tags:
  - Health
responses:
  200:
    description: API is healthy
    schema:
      type: object
      properties:
        status:
          type: string
          example: healthy
        model_loaded:
          type: boolean
          example: true
        database:
          type: string
          example: connected
        timestamp:
          type: string
          example: "2026-01-02T12:00:00"
