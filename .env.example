DATABASE_URL=sqlite:///predictions.db
SECRET_KEY=change-me-to-a-secure-value
FLASK_ENV=production
RATELIMIT_STORAGE_URL=memory://
CORS_ORIGINS=*
# Optionally set other environment variables used by config.py
# LOG_LEVEL=INFO
# .env.example - Copy to .env and fill in values

# Flask Configuration
FLASK_ENV=development
SECRET_KEY=your-secret-key-here
PORT=5000

# Database Configuration
DATABASE_URL=sqlite:///predictions.db
# For PostgreSQL: postgresql://user:password@localhost/dbname

# Model Configuration
MODEL_DIR=models
DEFAULT_MODEL=loan_model_v2.pkl

# Cache Configuration
CACHE_TYPE=simple
CACHE_DEFAULT_TIMEOUT=300

# Rate Limiting
RATELIMIT_STORAGE_URL=memory://
SECRET_KEY=change-me
FLASK_ENV=development
DEV_DATABASE_URL=sqlite:///predictions.db
DATABASE_URL=
CORS_ORIGINS=*
MODEL_DIR=models
DEFAULT_MODEL=loan_model_v2.pkl
FEATURE_NAMES_FILE=feature_names.txt
MODEL_INFO_FILE=model_info.json
RATELIMIT_DEFAULT="200 per hour, 1000 per day"
RATELIMIT_STORAGE_URL=memory://
CACHE_DEFAULT_TIMEOUT=300
MAX_REQUEST_TIMES=1000
# Flask Configuration
SECRET_KEY=dev-secret-key-change-in-production

# Database Configuration (Local Development)
DATABASE_URL=sqlite:///predictions.db

# Optional: For production with PostgreSQL on Render
# DATABASE_URL=postgresql://user:password@host:port/dbname

# Flask Environment
FLASK_ENV=development
FLASK_DEBUG=False

# Port (Render sets this automatically)
# PORT=5000

# Model paths (usually not needed - code has defaults)
# MODEL_PATH=models/loan_model_v2.pkl
# FEATURE_NAMES_PATH=models/feature_names.txt
# MODEL_INFO_PATH=models/model_info.json
